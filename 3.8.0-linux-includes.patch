--- origsrc/cfe-3.8.1.src/lib/Driver/ToolChains.cpp	2016-02-16 13:56:48.000000000 -0600
+++ src/cfe-3.8.1.src/lib/Driver/ToolChains.cpp	2016-07-20 18:33:39.986370400 -0500
@@ -3831,7 +3831,7 @@ std::string Linux::computeSysRoot() cons
   if (!getDriver().SysRoot.empty())
     return getDriver().SysRoot;
 
-  if (!GCCInstallation.isValid() || !isMipsArch(getTriple().getArch()))
+  if (!GCCInstallation.isValid())
     return std::string();
 
   // Standalone MIPS toolchains use different names for sysroot folder
@@ -3842,6 +3842,9 @@ std::string Linux::computeSysRoot() cons
   const StringRef TripleStr = GCCInstallation.getTriple().str();
   const Multilib &Multilib = GCCInstallation.getMultilib();
 
+  if (!isMipsArch(getTriple().getArch()))
+    return std::string("/usr/" + TripleStr.str() + "/sys-root");
+
   std::string Path =
       (InstallDir + "/../../../../" + TripleStr + "/libc" + Multilib.osSuffix())
           .str();
@@ -4106,6 +4109,7 @@ void Linux::AddClangCXXStdlibIncludeArgs
       InstallDir.str() + "/include/g++-v" + Version.MajorStr + "." +
           Version.MinorStr,
       InstallDir.str() + "/include/g++-v" + Version.MajorStr,
+      InstallDir.str() + "/include/c++",
       // Android standalone toolchain has C++ headers in yet another place.
       LibDir.str() + "/../" + TripleStr.str() + "/include/c++/" + Version.Text,
       // Freescale SDK C++ headers are directly in <sysroot>/usr/include/c++,
